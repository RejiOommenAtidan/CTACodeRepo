const styles = theme => ({
  'portal-maps-info-box': {
    background: theme.palette.primary.light,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: theme.palette.common.white,
    borderRadius: 10,
    width: 100,
    padding: 10,
    fontSize: 12
  }
});

export default styles;
